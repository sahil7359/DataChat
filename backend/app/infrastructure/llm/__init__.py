"""LLM provider gateway: vendor adapters, a resilience decorator stack, a
circuit breaker, and a strategy-based router. Adding a vendor is a new adapter
plus a config line — nothing here or upstream changes (Open/Closed)."""
